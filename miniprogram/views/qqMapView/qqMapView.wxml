
<map id="map" longitude="{{markers[0].longitude}}" latitude="{{markers[0].latitude}}" scale="19" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="showModal" circles="{{circles}}" polyline="{{polyline}}" polygons="{{polygons}}"
  enable-rotate="true" show-location style="width: 100%; height: 65vh" show-compass></map>